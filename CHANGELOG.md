# 版本记录

版本号统一在 `gradle.properties` 的 `appVersionCode`、`appVersionName` 维护。每次发布必须递增 `appVersionCode`，沿用 `.signing/paperpdf.jks` 原签名密钥；`build.ps1` 自动按版本号命名交付 APK。旧版本安装包保留在 `delivery`。

## 1.4.0（versionCode 5，2026-09-15）

- 单页/连续阅读轻触 PDF 可隐藏或呼出全部阅读工具栏及系统栏；隐藏时按返回先恢复工具栏。双击仍用于缩放，拖动、翻页、画笔/荧光/文字操作不触发显隐。
- 屏幕方向跟随系统自动旋转设置（fullUser），支持横屏及反向横屏，尊重系统旋转锁。
- 单页横屏默认适合页宽，长页上下拖动阅读；横屏收起提示和副标题，工具栏保持上下布局，不覆盖页面左右内容。
- 根据系统导航栏、挖孔/刘海和键盘 Insets 留出安全区域；旋转与显隐复用阅读视图，保留当前批注、缩放与连续阅读页内位置。
- 保留 1.3.0 原 APK；补充实体安卓机验证包（5 份/12 页确定性 PDF、清单、结果模板、独立写入故障提供器）及 1.4.0 专属验收补充。

## 1.3.0（versionCode 4，2026-09-14）

- 连续阅读支持双指缩放（1–5 倍）、双击放大/还原、放大后拖动和惯性滚动。
- 滚动模式默认检测并收起 PDF 左右白边，使正文适合手机屏幕宽度；设置可关闭，PDF 文件本身不裁剪、不重排。
- 新增“保存 → 覆盖原文件”；请求读写授权，写入前备份原始字节，完成后校验，部分写入失败时尝试恢复原文件。
- 阅读页图标按钮移除多余文字内边距，修复设置图标偏移、裁剪，标题保留省略显示。
- 夜间模式同步顶部、底部、页码、工具栏、系统状态栏、导航栏及弹窗颜色。
- 按文件分别记住页码，连续阅读额外记住页内位置；最近打开、目录、外部打开和草稿恢复均可恢复位置。
- 连续阅读使用有限位图缓存并释放离开的视图，避免长时间阅读累积所有页面位图。
- 沿用原包名和发布签名，可直接覆盖安装 1.2.0；保留旧 APK。

## 1.1.0（versionCode 2，2026-09-13）

- 新增微信 / QQ PDF 入口：系统下载目录选择，以及授权可访问文件夹后的应用内搜索和浏览。
- 支持从微信 / QQ 通过 PDF、`application/x-pdf`、`application/octet-stream` 类型的“打开方式”或“分享”进入应用。
- 编辑工具栏按钮从 82×48 dp 缩至 66×38 dp，工具栏整体高度从 68 dp 缩至 46 dp，给 PDF 页面留出更多空间。
- 首页去掉试读示例入口，底部文案只保留“文件留在设备上”。
- 将版本号集中管理；保持原包名和发布签名，可以覆盖安装 1.0.0。

## 1.2.0（versionCode 3，2026-09-13）

- 首页保持简洁，右上角新增统一设置入口；目录、微信 / QQ 文件夹和阅读偏好集中在设置中管理。
- “打开手机中的 PDF”会扫描并列出所有已授权目录中的 PDF，支持文件名搜索和按最近修改时间排序。
- 阅读页新增连续滚动模式，可上下滑动浏览整份文档；默认单页模式继续支持左右滑动翻页，两种模式可在设置或右上角菜单切换。
- 连续滚动模式按需渲染页面，长文档不会一次性加载全部位图；画笔、荧光和文字工具会提示切回单页模式，页面管理仍可使用。
- 版本号递增至 1.2.0（versionCode 3），沿用原发布签名，可覆盖安装 1.1.0。

## 1.0.0（versionCode 1，2026-09-09）

- 首版：本地 PDF 阅读、画笔/荧光/文字批注、页面操作、搜索、另存和分享。
